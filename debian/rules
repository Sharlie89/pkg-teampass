#!/usr/bin/make -f
# -*- makefile -*-
# Sample debian/rules that uses debhelper.
# This file was originally written by Joey Hess and Craig Small.
# As a special exception, when this file is copied by dh-make into a
# dh-make output file, you may use that output file without restriction.
# This special exception was added by Craig Small in version 0.37 of dh-make.

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

#TP_CONTENT = TeamPass-master/

%:
	dh $@ 

#override_dh_auto_build:
#	for i in $$(find debian/languages/ -name *.po); do \
#                msgfmt $$i -o $${i%%.po}.mo; \
#	done
#	pod2man --utf8 --center  install TeamPass-master/install 

#override_dh_install:
#	dh_install -Xlicense.txt -Xlicense.commercial.txt -XLICENSE -XCOPYING

#override_dh_fixperms:
#	dh_fixperms
#	chown www-data:www-data $(TP_CONTENT)

# Use like this: debian/rules get-orig-source VERSION=3.4
#get-orig-source:
#	@test -n "$(VERSION)" || (echo "Please set VERSION on the command-line. Ex: debian/rules get-orig-source VERSION=3.4" >&2; exit 1)
#	@test ! -e ../teampass_$(VERSION)+dfsg.orig.tar.xz || (echo "You already have ../teampass__$(VERSION)+dfsg.orig.tar.xz" >&2; exit 1)
#	uscan --noconf --force-download --rename --download-version $(VERSION) --destdir=.
#	tar --transform "s,^.[^/]*,teampass-$(VERSION)/," -xzvf \
#               ./teampass_$(VERSION).orig.tar.gz
#        rm ./wordpress_$(VERSION).orig.tar.gz
#        cd wordpress-$(VERSION) && rm $(UPSTREAM_FILE_BLACKLIST)
#	tar -acvf ../teampass_$(VERSION)+dfsg.orig.tar.xz teampass-$(VERSION)
#	rm -rf teampass-$(VERSION)
#	@echo "Successfully created ../teampass_$(VERSION)+dfsg.orig.tar.xz"
